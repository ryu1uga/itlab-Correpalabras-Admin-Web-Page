services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8002:3000" 
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development